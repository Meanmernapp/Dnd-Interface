import React from 'react'

const Button = () => {
    return (
        <button style={{
            width: "10rem",
            height: "2rem",
            border: "1px dashed",
            borderRadius: "20px",
        }} > Add quick reply </button >
    )
}

export default Button